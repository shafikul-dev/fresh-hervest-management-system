export const config = {
  apiBaseUrl: process.env.NEXT_PUBLIC_API_BASE_URL || 'https://your-live-server-url.com',
  appName: process.env.NEXT_PUBLIC_APP_NAME || 'SM Technology',
  appVersion: process.env.NEXT_PUBLIC_APP_VERSION || '1.0.0',
}
